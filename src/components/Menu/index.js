import { connect } from 'react-redux'

import MenuView from './MenuView'

const mapDispatchToProps = (dispatch) => {
    return {}
}

const mapStateToProps = (dispatch) => {
    return {}
}

const Menu = connect(mapStateToProps, mapDispatchToProps)(MenuView)

export default Menu
